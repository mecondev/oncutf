[project]
name = "oncutf"
version = "1.3.0"
description = "Advanced Batch File Renamer with EXIF Metadata Support and Professional UI"
authors = [
    {name = "Michael Economou", email = "info@oncut.gr"}
]
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
keywords = ["file-renamer", "batch-rename", "exif", "metadata", "photography", "pyqt5"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",

    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Graphics :: Graphics Conversion",
    "Topic :: System :: Filesystems",
    "Topic :: Utilities",
]
dependencies = [
    "PyQt5>=5.15.11",
    "PyQt5-Qt5>=5.15.15",
    "PyQt5-sip>=12.15.0",
    "Pillow>=9.0.0",
    "charset-normalizer>=3.0.0",
    "psutil>=6.1.0",
    "aiofiles>=24.1.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.5",
    "pytest-qt>=4.4.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.14.1",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
    "black>=24.0.0"
]
test = [
    "pytest>=8.3.5",
    "pytest-qt>=4.4.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.14.1"
]

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["core*", "models*", "modules*", "utils*", "widgets*"]
exclude = ["tests*", "docs*", "examples*", "scripts*", "temp*", "backups*", "reports*", "logs*", "assets*", "resources*"]

[project.urls]
Homepage = "https://github.com/mecondev/oncutf"
Repository = "https://github.com/mecondev/oncutf"
Documentation = "https://github.com/mecondev/oncutf/tree/main/docs"
Issues = "https://github.com/mecondev/oncutf/issues"

# --------------------------
# mypy configuration
# --------------------------
[tool.mypy]
python_version = "3.12"
# Gradual typing strategy: Start with basic checks, progressively enable stricter rules
strict = false
ignore_missing_imports = true
warn_unused_configs = true
warn_unused_ignores = true

# Baseline type safety (enabled globally)
warn_return_any = true
check_untyped_defs = true
warn_redundant_casts = true
no_implicit_optional = true
warn_unreachable = true

# Disabled globally (enable per-module as code improves)
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_calls = false

# Exclude non-package directories
exclude = '.*(backups|\.cache|temp|reports|scripts|examples)/'

# =============================================================================
# Third-party libraries
# =============================================================================
[[tool.mypy.overrides]]
module = ["PyQt5.*", "PIL.*", "psutil.*"]
ignore_missing_imports = true
ignore_errors = true

# =============================================================================
# Legacy code with minimal type hints (ignore errors for now)
# =============================================================================
[[tool.mypy.overrides]]
module = ["oncutf.ui.*", "oncutf.utils.*", "tests.*", "main"]
ignore_errors = true

# =============================================================================
# Core modules - GRADUAL STRICTNESS
# =============================================================================

# Phase 1: Well-typed modules with strict checking
[[tool.mypy.overrides]]
module = [
    "oncutf.core.type_aliases",
    "oncutf.core.application_context",
    "oncutf.core.preview_manager",
]
ignore_errors = false
warn_return_any = true
check_untyped_defs = true
disallow_any_explicit = false  # Allow explicit Any for now

# Phase 2: Core modules with basic type checking (errors enabled, but lenient)
[[tool.mypy.overrides]]
module = [
    "oncutf.core.file_store",
    "oncutf.core.selection_store",
    "oncutf.core.metadata_staging_manager",
    # Promoted from Phase 3 (all have 0 errors!)
    "oncutf.core.unified_metadata_manager",
    "oncutf.core.structured_metadata_manager",
    "oncutf.core.unified_rename_engine",
    "oncutf.core.batch_operations_manager",
    "oncutf.core.memory_manager",
]
ignore_errors = false
disallow_untyped_defs = false

# Phase 3: Remaining core modules with type issues (still ignored, to be migrated)
[[tool.mypy.overrides]]
module = [
    "oncutf.core.persistent_metadata_cache",
    "oncutf.core.unified_column_service",
    "oncutf.core.database_manager",
    "oncutf.core.conflict_resolver",
    "oncutf.core.drag_visual_manager",
    "oncutf.core.rename_history_manager",
    "oncutf.core.performance_monitor",
    "oncutf.core.drag_manager",
    "oncutf.core.parallel_metadata_loader",
    "oncutf.core.parallel_hash_worker",
    "oncutf.core.theme_manager",
    "oncutf.core.hash_manager",
    "oncutf.core.persistent_hash_cache",
    "oncutf.core.advanced_cache_manager",
    "oncutf.core.metadata_command_manager",
    "oncutf.core.batch_processor",
    "oncutf.core.pyqt_imports",
    "oncutf.core.modifier_handler",
    "oncutf.core.metadata_commands",
    "oncutf.core.filesystem_monitor",
]
ignore_errors = true

# Controllers, models, modules - keep ignored for now
[[tool.mypy.overrides]]
module = ["oncutf.controllers.*", "oncutf.models.*", "oncutf.modules.*"]
ignore_errors = true

# =============================================================================
# Domain package - STRICT MODE (reference implementation)
# =============================================================================
[[tool.mypy.overrides]]
module = ["oncutf.domain.*"]
strict = true
warn_return_any = true
check_untyped_defs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true

# =============================================================================
# Migration Plan:
# 
# 1. ✅ Phase 1 (3 modules): Strict checking enabled
#    - type_aliases, application_context, preview_manager
# 
# 2. ✅ Phase 2 (8 modules): Basic checking enabled  
#    - file_store, selection_store, metadata_staging_manager
#    - unified_metadata_manager, structured_metadata_manager
#    - unified_rename_engine, batch_operations_manager, memory_manager
# 
# 3. ⏳ Phase 3 (20 modules): Errors still ignored (206 errors to fix)
#    - persistent_metadata_cache, unified_column_service, database_manager
#    - conflict_resolver, drag_visual_manager, rename_history_manager
#    - performance_monitor, drag_manager, parallel_metadata_loader
#    - parallel_hash_worker, theme_manager, hash_manager
#    - persistent_hash_cache, advanced_cache_manager
#    - metadata_command_manager, batch_processor, pyqt_imports
#    - modifier_handler, metadata_commands, filesystem_monitor
# 
# Progress: 11/31 core modules now have mypy checking enabled (35%)
# =============================================================================

# --------------------------
# Ruff configuration
# --------------------------
[tool.ruff]
target-version = "py312"
line-length = 100
fix = true
exclude = ["backups", ".cache", "temp", "reports", "docs", "venv", "__pycache__", "*.pyc", "*.md"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "ARG", # flake8-unused-arguments
    "PIE", # flake8-pie
    "T20", # flake8-print
    "PYI", # flake8-pyi
    "G",   # flake8-logging-format (enforce lazy logging)
    "PLR", # pylint refactors (complexity, design patterns)
]
ignore = [
    "E501",   # line too long (handled by black)
    "N802",   # function name should be lowercase
    "F403",   # wildcard import
    "F405",   # name may be undefined
    "E402",   # module level import not at top
    "B008",   # do not perform function calls in argument defaults
    "C901",   # too complex
    "SIM102", # nested if statements
    "TC001", # typing imports should be in TYPE_CHECKING block
    "TC002", # typing imports should be in TYPE_CHECKING block
    "TC003", # typing imports should be in TYPE_CHECKING block
    # PLR ignores - keep useful rules, ignore overly strict ones
    "PLR2004", # magic-value-comparison (too noisy for constants like 0, 1, 2)
    "PLR0912", # too-many-branches (legacy code, will refactor gradually)
    "PLR0913", # too-many-arguments (common in PyQt signals/slots)
    "PLR0915", # too-many-statements (legacy __init__ methods)
]

# Per-file ignores for scripts and utilities
[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["T201"]  # Allow print statements in scripts
"tests/*.py" = ["T201"]     # Allow print statements in tests

[tool.ruff.lint.isort]
force-single-line = false
combine-as-imports = true
known-first-party = ["core", "models", "modules", "utils", "widgets"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

# --------------------------
# Pytest
# --------------------------
[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --tb=short --strict-markers --strict-config"
testpaths = ["tests"]
qt_api = "pyqt5"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "gui: marks tests that require GUI (skipped on CI)",
    "exiftool: marks tests that require exiftool",
    "local_only: marks tests that only run in local environment"
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
    "ignore::UserWarning:PyQt5.*"
]

# --------------------------
# Coverage
# --------------------------
[tool.coverage.run]
source = [".", "core", "models", "modules", "utils", "widgets"]
omit = [
    "tests/*",
    "venv/*",
    "*/site-packages/*",
    "setup.py",
    "conftest.py",
    "backups/*",
    "temp/*",
    "reports/*",
    "*.pyc",
    "__pycache__/*"
]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod"
]

# ----------------------------------------
# Black configuration
# ----------------------------------------
[tool.black]
line-length = 100
target-version = ['py312']
include = '\.pyi?$'
extend-exclude = '/(\.eggs|\.git|\.hg|\.mypy_cache|\.tox|\.venv|build|dist|backups|temp|reports)/'

# ----------------------------------------
# Pre-commit hooks configuration
# ----------------------------------------
# Note: Pre-commit configuration moved to .pre-commit-config.yaml
# ----------------------------------------
